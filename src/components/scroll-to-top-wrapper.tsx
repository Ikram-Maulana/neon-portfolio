"use client";

import { FC } from "react";
import ScrollToTop from "react-scroll-to-top";

const ScrollToTopWrapper: FC = () => {
  return <ScrollToTop smooth />;
};

export default ScrollToTopWrapper;
